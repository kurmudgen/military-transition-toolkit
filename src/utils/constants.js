/**
 * App-wide constants
 */

// Shutdown banner messaging - single source of truth
export const SHUTDOWN_BANNER = {
  line1: 'Premium features free during shutdown with login',
  line2: 'Core tools always free - no account required'
}

// Auth loading timeout (milliseconds)
export const AUTH_LOADING_TIMEOUT = 4000 // 4 seconds
